# ===== Refactor Agent inputs =====
REFAC_PROJECT=data/marketing-demo
REFAC_CONTEXT_PACK=context_pack.json

# 这行建议用引号，因为有空格
REFAC_REQUEST="Reduce complexity in decideOffer by extracting the risk control layer into private helper methods. Keep behavior unchanged."

REFAC_MAX_ITERS=3


# Docker sandbox (可选)
REFAC_USE_DOCKER=false
REFAC_DOCKER_IMAGE="maven:3.9-eclipse-temurin-17"

# ===== LLM config =====
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_BASE_URL=

REFAC_ALLOW_CMDS="git;rg;mvn;java;javac"
REFAC_VERIFY_CMDS="mvn -q test"

